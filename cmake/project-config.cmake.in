@PACKAGE_INIT@
set (_old_PACKAGE_PREFIX_DIR "${PACKAGE_PREFIX_DIR}")
set (_old_CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH})
set (CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${PACKAGE_PREFIX_DIR}/cmake")
find_package (Boost REQUIRED COMPONENTS filesystem program_options random chrono thread)
find_package (ZeroMQ REQUIRED)
find_package (FMILIB REQUIRED)
find_package (LIBZIP REQUIRED)
include ("CompatFindProtobuf")
set (CMAKE_MODULE_PATH ${_old_CMAKE_MODULE_PATH})
unset (_old_CMAKE_MODULE_PATH)
set (PACKAGE_PREFIX_DIR "${_old_PACKAGE_PREFIX_DIR}")
unset (_old_PACKAGE_PREFIX_DIR)

include ("@PACKAGE_exportFileConfig@")
set (coral_LIBRARIES "coral")
set_and_check (coral_INCLUDE_DIRS "@PACKAGE_includeDirsConfig@")
