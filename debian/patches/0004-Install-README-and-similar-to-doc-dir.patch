From: "Lars T. Kyllingstad" <lars.kyllingstad@sintef.no>
Date: Tue, 28 Feb 2017 14:17:41 +0100
Subject: Install README and similar to doc dir

---
 CMakeLists.txt              | 6 ++----
 cmake/build-info-file.cmake | 2 +-
 2 files changed, 3 insertions(+), 5 deletions(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index f0a7223..b720060 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -55,11 +55,9 @@ set (headerInstallDir "include")
 if (WIN32)
     set (cmakeFilesInstallDir "cmake")
     set (docInstallDir        "doc")
-    set (readmeInstallDir     "doc")
 else ()
     set (cmakeFilesInstallDir "share/${CMAKE_PROJECT_NAME}/cmake")
     set (docInstallDir        "share/doc/${CMAKE_PROJECT_NAME}")
-    set (readmeInstallDir     "share/${CMAKE_PROJECT_NAME}")
 endif ()
 
 # The export name to use for libraries.
@@ -192,9 +190,9 @@ install (EXPORT ${exportTarget} DESTINATION ${cmakeFilesInstallDir})
 
 # Other files
 set (readmeFile "${CMAKE_SOURCE_DIR}/README.md")
-install (FILES ${readmeFile} DESTINATION ${readmeInstallDir})
+install (FILES ${readmeFile} DESTINATION ${docInstallDir})
 set (licenceFile "${CMAKE_SOURCE_DIR}/LICENCE.txt")
-install (FILES ${licenceFile} DESTINATION ${readmeInstallDir})
+install (FILES ${licenceFile} DESTINATION ${docInstallDir})
 install (DIRECTORY "${CMAKE_SOURCE_DIR}/manuals/" DESTINATION ${docInstallDir})
 
 install (FILES
diff --git a/cmake/build-info-file.cmake b/cmake/build-info-file.cmake
index 32fb118..0fa11c8 100644
--- a/cmake/build-info-file.cmake
+++ b/cmake/build-info-file.cmake
@@ -22,4 +22,4 @@ add_custom_command(
     VERBATIM
 )
 add_custom_target("build-info" ALL DEPENDS "${_buildInfoFile}")
-install(FILES "${_buildInfoFile}" DESTINATION "${readmeInstallDir}")
+install(FILES "${_buildInfoFile}" DESTINATION "${docInstallDir}")
